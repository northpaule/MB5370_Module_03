---
title: "Workshop.04"
author: "Paule Mathieu"
format: html
editor: visual
---

## Assembling the Metagenome of Black Band Disease

Generating the metagenome of BBD from *Acropora hyacynthus* at Orpheus Island. Created a symbolic link between BBD data and raw_reads directory. Inside *flye* directory, modified a copy of run_flye.sh script (\> run_flye_bbd.sh) and submitted slurm to sbatch through:

``` bash
sbatch run_flye_bbd.sh
```

Step 2. Run metabat on flye_bbd assembly

``` bash
cd ~/MB5370_Module_03/metabat
# Extract depth information
cat ../flye/bbd/assembly_info.txt  | grep -v '#' | awk '{OFS="\t";print $1,$3}' > depths_bbd.txt
# Reorder contigs based on depth
cat depths_bbd.txt | awk '{print $1}' | xargs samtools faidx ../flye/bbd/assembly.fasta > assembly_bbd_ordered.fasta
# run metabat
metabat2 -i assembly_bbd_ordered.fasta -a depths_bbd.txt --cvExt -o bins_bbd/metabat --seed 5
```

Step 3. Run checkM on bins created by metabat. \*subsitute XX with nb of largest contig edge_121

``` bash
cd ~/MB5370_Module_03/checkm
mkdir bins_bbd
cp ../metabat/bins_bbd/*.fa bins_bbd/
samtools faidx ../flye/bbd/assembly.fasta contig_XX > bins_bbd/contig_XX.fa
```

Step 4. Create a new slurm shell script to send your checkM.

``` bash
run_checkm.bbd.sh

# Submit batch
sbatch run_checkm_bbd.sh
```

### Functonial Analysis of the BBD Metagenome

| Analysis Step | File/Folder                   |
|---------------|-------------------------------|
| Flye          | flye/bbd                      |
| MetaBAT       | metabat/bins_bbd              |
| CheckM        | checkm/checkm_results_bbd.txt |

Step 5: Bin checkm based on results

We isolated the bins with a completeness higher than 15% (into bbd_topbins.txt) using *cat.*

Step 6: Perform taxonomic classification with *gtdbtk*

We ran a classification workflow.

Step 7. Find and annotate genes with *prokka*

We identified the genes present within the genomes of bbd taxa.

Step 8. Infer metabolic pathways

We used *minpath* to take functional info for individual genes and use this to infer the presence of broader biological pathways (involving multiple genes/steps). We ran this on each of the genomes (bins) in our analysis. Minpath looked at all of the annotated genes in our data and infer the minimal set of biological pathways that can explain the presence of all these genes.

Photosynthesis light reaction **PWY-101** includes essential components of photosynthesis that occur in light, Photosystem I and Photosystem II.

Superpathway of tetrathionate reduction **PWY-5360** includes enzymes required to reduce sulfite to hydrogen sulfide. Note that sulfite acts as the terminal electron acceptor in this pathway, however, in a broader sense the term “sulfate reducing” refers to micro-organisms that can produce sulfide from a range of oxidised sulfur compounds.

**Question 4.1**

How many bins contain at least one gene from this pathway?

``` bash
Four bins have at least one gene from this path: contig_121, metabat.25, metabat.48, metabat.55.
```

Which bin has all of the genes (ie a complete pathway)?

``` bash
Metabat.55 has all of the genes (4/4).
```

**Question 4.2**

How many bins contain the pathway PWY-5360?

``` bash
Only one bin is found in this pathway: metabat.8.
```

What family (in a taxonomic sense) do these bins belong to?

``` bash
*Salmonella typhimurium*
```
